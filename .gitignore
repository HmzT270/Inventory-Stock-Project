# LOG VE GEÇİCİ DOSYALAR
*.log                    # Uygulama log dosyaları
logs/                    # Log klasörü
*.tmp                    # Geçici dosyalar
*.bak                    # Yedek dosyalar
*.swp                    # Editör geçici dosyaları (Vim, VS Code)
*.db                     # Geçici veritabanı dosyaları

# FRONTEND (React, Vite, Node)
node_modules/            # Node bağımlılıkları (npm install ile yeniden kurulur)
dist/                    # Üretim çıktısı
dist-ssr/                # Server-side render çıktısı
.vite/                   # Vite önbellek klasörü
.env                     # Ortam değişkenleri (gizli anahtarlar içerir)

# BACKEND (.NET Core)
bin/                     # Derleme çıktısı
obj/                     # Derleme önbelleği
.vs/                     # Visual Studio klasörü
appsettings.*.json       # Ortam yapılandırmaları (şifre içerebilir)
*.suo                    # VS kullanıcı ayar dosyası
*.user                   # VS kullanıcı ayar dosyası

# EDITÖR VE SİSTEM DOSYALARI
.vscode/                 # VS Code proje ayarları
!.vscode/extensions.json # Belirli uzantı listesi dahil olabilir
.idea/                   # JetBrains (WebStorm/Rider) ayar klasörü
.DS_Store                # macOS sistem dosyası
Thumbs.db                # Windows sistem önizleme dosyası
desktop.ini              # Windows sistem konfigürasyon dosyası

# ARŞİV DOSYALARI
*.zip
*.rar
*.7z